import 'package:flutter/material.dart';

class ReportArea extends StatelessWidget {
  const ReportArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Report Area'),),
      body: ListView(),
    );
  }
}