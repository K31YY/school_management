import 'package:flutter/material.dart';

class ViewsTeacher extends StatelessWidget {
  const ViewsTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Views Teacher'),),
      body: ListView(),
    );
  }
}