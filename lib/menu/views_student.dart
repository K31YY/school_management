import 'package:flutter/material.dart';

class ViewsStudent extends StatelessWidget {
  const ViewsStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Views Student'),),
      body: ListView(),
    );
  }
}